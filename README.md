# Multi-Area-OSPF-
The project includes inter-area and intra-area routing with area 0 as backbone area, area 1 and area 2 in play.
Ip addresses assigned to each router's interfaces.
R1 and R4 are ABRs(Area Border Routers).
The OSPF network type used is BROADCAST.
OSPF configured on all routers.
pings work properly.
